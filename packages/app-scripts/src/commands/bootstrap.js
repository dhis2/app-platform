const handler = () => {

}

const command = {
  command: 'bootstrap',
  desc: 'Bootstrap a copy of the DHIS2 app-shell to wrap the current application component',
  handler
}

module.exports = command
