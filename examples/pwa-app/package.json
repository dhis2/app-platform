{
    "name": "pwa-app",
    "version": "1.0.0",
    "description": "",
    "license": "BSD-3-Clause",
    "private": true,
    "scripts": {
        "build": "d2-app-scripts build",
        "start": "d2-app-scripts start",
        "test": "d2-app-scripts test",
        "deploy": "d2-app-scripts deploy",
        "serve": "serve -l 5500 build/app",
        "demo": "yarn install --force && yarn build --force --verbose && yarn serve"
    },
    "devDependencies": {
        "@dhis2/cli-app-scripts": "^6.2.0"
    },
    "peerDependencies": {
        "@dhis2/d2-i18n": "*",
        "react": "^16.8",
        "react-dom": "^16.8"
    },
    "dependencies": {
        "@dhis2/app-service-offline": "file:../../../app-runtime/services/offline"
    },
    "resolutions": {
        "@dhis2/app-service-offline": "file:../../../app-runtime/services/offline"
    }
}
